<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>


<ol>
  <li>
    <%= link_to "Edit task",
    edit_task_path(@task) %>
  </li>
  <li>
  <%= link_to "Back to all tasks",
    tasks_path(@task) %>
  </li>
  <li>
  <%= link_to "Delete",
    tasks_path(@task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  </li>
</ol>
